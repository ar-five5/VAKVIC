{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "npm run migrate:up && npm run seed && npm run start",
    "healthcheckPath": "/api/v1/health"
  }
}
